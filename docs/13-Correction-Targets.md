# 13 — Correction Targets

QA does not write the corrected prompt.

It outputs structured target:
- domain;
- priority;
- objective;
- issue refs;
- passing constraints to preserve;
- do-not-change list.

Example:
“Prevent hand contact with neckline while preserving existing camera, background and color treatment.”
